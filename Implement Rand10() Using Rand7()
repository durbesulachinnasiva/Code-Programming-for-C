// Provided by LeetCode
int rand7();

int rand10() {
    while (1) {
        int num = (rand7() - 1) * 7 + rand7();  // 1 to 49
        if (num <= 40)
            return 1 + (num - 1) % 10;          // uniform 1..10
    }
}

